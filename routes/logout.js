/**
 * Created by KESHU on 10-Dec-15.
 */

/*var mongoose = require('mongoose');
var user = require('../routes/models');
// Logout the user, then redirect to the home page.
app.get('/logout', function(req, res) {
    req.logout();
    res.redirect('/');
});
*/